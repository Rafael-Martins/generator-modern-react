import React, { createContext } from 'react';

const <%= contextName %>Context = createContext();

const <%= contextName %>Provider = ({ children }) => {
  const contextValue = {};

  return (
    <<%= contextName %>Context.Provider value={contextValue}>
      {children}
    </<%= contextName %>Context.Provider>
  );
};

export { <%= contextName %>Context, <%= contextName %>Provider };